                              DIFFERNCE BETWEEN HTTP1.1 vs HTTP2

                                          HTTP/1.1

 1.HTTP/1.1 loads resources one ofter the other, so if one resoure cannot be loaded, it blocks all the other resource
   behind.

                                          HTTP/2

1.HTTP/2 is able to use a single Tcp connection to send multiple streams of data at once so that no one resource blocks any     
  other resource.

                          OBJECTS AND ITS INTERNAL REPRESENTATION IN JAVASCRIPT

1. In javascript, objects are collections of key-value pairs, where keys are strings (or symblos) and values can be of any 
   data type, including other objects.
2. Objects are used to represent real-world entities, data structures, and more complex data types.

                                         IP ADDRESS

1. An IP adddress is a unique address that identifies a device on the internet or local network.
2. IP stands for "internet protocol", which is the set of rules governing the format of data sent via the internet or 
   local network. 

              